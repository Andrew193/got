import { createAction } from '@ngrx/store';

export const showHeroPreview = createAction('[Daily reward] showHeroPreview');
export const hideHeroPreview = createAction('[Daily reward] hideHeroPreview');
export const toggleHeroPreview = createAction('[Daily reward] toggleHeroPreview');
